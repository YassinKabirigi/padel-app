package be.ephec.padel_backend.controller;

import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.Disabled;

/**
 * Test d'intégration désactivé — nécessite une base de données réelle.
 * Les règles métier sont testées dans ReservationServiceTest.
 */
@Disabled("Requiert une base SQL Server active — lancé manuellement")
class MatchControllerIntegrationTest {

    @Test
    void placeholder() {
        // Ce test est désactivé, voir ReservationServiceTest pour les tests unitaires
    }
}
